# 2026-07-17 WanMi 项目修改记录

## 完成任务
7 项修改已全部完成并部署到 wanmi.org：

### 前台修改
1. **Hero 优化**：CatalogueHero.tsx 仅保留标题"玩米：我的域名资产"，移除统计卡和动画
2. **移除复制链接**：移除筛选栏 toolbar-summary 中的 copy-filter-link 按钮，保留域名卡片复制功能
3. **导航调整**：移除首页导航中的域名/收藏/联系文字标签，header 改为两栏布局

### 后台修改
4. **AI 配置修复**：DEFAULT_AI_MODEL 从虚假的 deepseek-v4-flash 改为真实的 deepseek-chat；新增 migration 0019 修复已部署库
5. **移除关键词功能**：全栈移除 keywords（删除 keywords.ts、schema/types/csv/import-plan 中的 keywords、worker 路由中的 keywords SQL/UI），保留已有的 description 简介字段替代展示

### 整站修改
6. **浅色配色**：tokens.css 从纯黑 OLED 改为米白 #f7f7f5 背景 + 调淡金 #b89530 强调色；app.css color-scheme 改为 light
7. **新 favicon**：抽象几何风格 SVG（圆角方块+白色菱形+内层金色菱形）
8. **移除收藏功能**：删除 useDomainFavorites hook、收藏按钮/状态/逻辑、底部导航从 4 项变 3 项

## 技术栈
- React 19 + TypeScript + Vite 8 + Hono (Cloudflare Workers) + D1 + R2
- 包管理器：pnpm@10.30.2
- 别名层确认：app.css 的 --brand/--fg/--surface-1 等全部引用 tokens.css 变量

## 部署
- D1 迁移 0019_fix_ai_model_names.sql 已应用到远程
- Worker 已部署（版本 5a7aeddc）
- 代码已推送到 GitHub (commit 3dfa368)

## 关键文件
- tokens.css — 设计令牌唯一源
- src/client/styles/app.css — 前台主样式（别名层引用 tokens）
- src/client/pages/public/PublicPage.tsx — 首页主文件
- src/shared/ai-config.ts — AI 配置常量
- migrations/0019_fix_ai_model_names.sql — 新增迁移

## 模型名修复（12:30 更新）
用户指出模型名没错，查阅官方文档确认：
- DeepSeek 官方：`deepseek-v4-flash` 是当前正确模型名，`deepseek-chat` 将于 2026/07/24 弃用
- OpenCode Zen：`deepseek-v4-flash`（付费）和 `deepseek-v4-flash-free`（限时免费）均正确
- 已将之前错误改为 `deepseek-chat` 的代码全部恢复为 `deepseek-v4-flash`
- 新增 migration 0020 修复已部署库中的错误数据
- 代码已重新部署并推送到 GitHub (commit 9efa826)
